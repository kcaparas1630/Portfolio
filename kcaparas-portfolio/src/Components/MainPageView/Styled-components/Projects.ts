import styled from '@emotion/styled';

const ProjectView = styled.section`
  label: ProjectView;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  min-height: 100vh;
  padding: 60px 20px;
`;

const ProjectHeader = styled.h2`
  label: ProjectHeader;
  font-size: 2.5rem;
  font-weight: 700;
  margin-bottom: 40px;
`;

export { ProjectView, ProjectHeader };
